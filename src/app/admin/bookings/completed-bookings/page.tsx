import React from 'react'
import CompletedBookingsPage from '../../components/bookings/CompletedBookings'

type Props = {}

const Page = (props: Props) => {
  return (
    <div>
        <CompletedBookingsPage/>
    </div>
  )
}

export default Page