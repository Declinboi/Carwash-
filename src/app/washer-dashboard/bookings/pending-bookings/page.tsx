import React from 'react'
import PendingBookingsPage from '../../components/bookings/PendingBookings'

type Props = {}

const Page = (props: Props) => {
  return (
    <div>
        <PendingBookingsPage/>
    </div>
  )
}

export default Page